[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "randomiser"
version = "0.1.0"
description = "Multi-source seed generator with selectable OTP, map, and maze outputs"
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "ManniAndBinni" }]
dependencies = [
    "numpy",
    "opencv-python",
    "sounddevice",
    "streamlit",
    "pandas",
    "pyyaml",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
