[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "randomiser"
version = "0.1.0"
description = "Multi-source entropy OTP generator and evaluation lab"
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "ManniAndBinni" }]
dependencies = [
    "streamlit",
    "pandas",
    "pyyaml",
]
