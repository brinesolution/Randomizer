mode: web
project_name: randomiser
environment: development
experiment:
  base_dir: data/experiments
  save_source_inputs: true
  save_output_index: true
sources:
  enabled:
    - camera
    - microphone
    - cpu_jitter
    - scheduler_jitter
  min_healthy_sources: 2
ui:
  show_source_inputs: true
  show_pipeline_trace: true
  show_otp_result: true
